/**
 * Enthält die Java-Umsetzungen der Datentypen, die NaMi in Form von
 * JSON-Objekten liefert. Die gelieferten JSON-Daten können mittels GSON
 * direkt in die entsprechenden Java-Objekte umgesetzt werden und umgekehrt.
 */
package nami.connector.namitypes;

